#include "Constants.h"

// ============================================================
//  ??nh ngh?a màu s?c – ch? ???c kh?i t?o DUY NH?T ? ?ây.
//  T?t c? các file khác ??u tr? v? b?n này qua t? khóa extern.
// ============================================================

const sf::Color BG_COLOR(25, 25, 30);               // N?n t?i Dark Mode
const sf::Color GRID_COLOR(70, 70, 80);             // L??i xám tro
const sf::Color COLOR_X(255, 50, 50);               // ?? Neon – quân X
const sf::Color COLOR_O(50, 255, 150);              // Xanh lá Neon – quân O
const sf::Color HOVER_COLOR(255, 255, 255, 30);     // Sáng m? khi hover (alpha = 30)
const sf::Color WIN_LINE_COLOR(255, 215, 0);        // Vàng n?i b?t – ???ng th?ng
const sf::Color BTN_COLOR(40, 40, 50);              // Màu n?n nút b?m